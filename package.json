{
  "name": "e2e-report-composer",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E runner with full-page screenshots and grouped HTML reports",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.2",
    "@types/node": "^25.5.0"
  }
}
